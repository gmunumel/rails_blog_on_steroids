require 'rails_helper'

describe Entities::ReviewEntity do
  describe 'fields' do
    subject(:subject) { Entities::ReviewEntity }
    #specify { expect(subject).to represent(:user)}
    #specify { expect(subject).to represent(:code_samples)}
  end
end