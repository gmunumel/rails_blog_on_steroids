require 'rails_helper'

describe Entities::PairProgrammingSessionsEntity do
  describe 'fields' do
    subject(:subject) { Entities::PairProgrammingSessionsEntity }
    #specify { expect(subject).to represent(:host_user)}
    #specify { expect(subject).to represent(:name)}
    #specify { expect(subject).to represent(:visitor_user)}
    #specify { expect(subject.reviews).to represent(:user)}


    # let!(:token) { create :authentication_token }
    # specify 'presents the first available token' do
    #   json = Entities::UserWithTokenEntity.new(token.user).as_json
    #   expect(json[:token]).to be_present
    # end
  end
end